[project]
name = "diffuser"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.13.0",
    "diffusers>=0.37.1",
    "safetensors>=0.7.0",
    "torch>=2.11.0",
    "torchvision>=0.20.0",
    "transformers>=5.5.4",
]
